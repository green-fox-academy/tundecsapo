'use strict';
export{};

let todoText: string = ' - Buy milk\n';
todoText = `My todo:\n${todoText} - Download games\n     - Diablo`;

console.log(todoText);
//or we can save the different parts into variables and concat into a string but it would be longer
