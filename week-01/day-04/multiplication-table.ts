'use strict';

let number: number = 15;

// Create a program that
// prints the multiplication table with number
for (let i: number = 1; i < 16; i++){let result: number = i * number;
    console.log(`${i}` + " * " + `${number}` + " = " + `${result}`);
}
